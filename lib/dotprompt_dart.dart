/// A library for working with .prompt files that contain structured prompts for
/// LLMs.
///
/// DotPrompt allows loading, parsing, and rendering prompt templates with
/// variables using a simple format that includes YAML front matter for metadata
/// and a Mustache template for the prompt content.
library;

export 'src/dot_prompt.dart';
