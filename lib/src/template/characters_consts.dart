/// Character code constants used in template parsing.
library;

/// Backslash character code (\\).
const backSlash = 92; // \
/// Forward slash character code (/).
const slash = 47; // /
/// Newline character code (\\n).
const enter = 10; // '\n'
/// Space character code.
const space = 32; // ' '
/// Open curly brace character code ({).
const openBracket = 123; // {
/// Close curly brace character code (}).
const closeBracket = 125; // }
/// Dollar sign character code ($).
const dollar = 36; // $
/// Hash/pound sign character code (#).
const sharp = 35; // #
/// Underscore character code (_).
const underscore = 95; // _
/// Dot/period character code (.).
const dot = 46; // .
/// Double quote character code (").
const quote = 34; // "
/// Single quote character code (').
const singleQuote = 39; // '
/// End-of-stream sentinel value.
const eos = -1; // '
/// Greater-than character code (>).
const more = 62; // >
/// Less-than character code (<).
const less = 60; // <
/// Equal sign character code (=).
const equal = 61; // =
/// Exclamation mark character code (!).
const exclMark = 33; // !
/// Caret character code (^).
const caret = 94; // ^
